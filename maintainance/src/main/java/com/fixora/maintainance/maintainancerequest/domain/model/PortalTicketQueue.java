package com.fixora.maintainance.maintainancerequest.domain.model;

/**
 * Portal list modes beyond generic status filtering.
 */
public enum PortalTicketQueue {
    /** Tickets the logged-in user may estimate (role + executor company + estimation actor). */
    NEEDS_ESTIMATION
}
